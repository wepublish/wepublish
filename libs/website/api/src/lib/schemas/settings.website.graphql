fragment WebsiteSettings on WebsiteSettings {
  analytics {
    googleAnalytics {
      enabled
      key
    }

    googleTagManager {
      enabled
      key
    }

    plausible {
      enabled
      key
    }

    piwik {
      enabled
      key
    }
  }

  ads {
    sparkLoop {
      enabled
      key
    }
  }

  theme
  fonts {
    weight
    style
    name
  }
}

query WebsiteSettings {
  websiteSettings {
    ...WebsiteSettings
  }
}
