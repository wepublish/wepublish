import { Preview } from '@storybook/nextjs';
import { decorators, parameters } from '@wepublish/storybook';

export default {
  decorators,
  {
    docs: {
      codePanel: true
    }
  },
} as Preview;
