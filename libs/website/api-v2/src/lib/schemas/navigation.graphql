fragment FullNavigation on Navigation {
    id
    key
    name
    links {
        ... on PageNavigationLink {
            label
            pageID
        }

        ... on ArticleNavigationLink {
            label
            articleID
        }

        ... on ExternalNavigationLink {
            label
            url
        }
    }
}

query NavigationList {
    getNavigations {
        ...FullNavigation
    }
}

query Navigation($slug: String!) {
    getNavigationByKey(key: $slug) {
        ...FullNavigation
    }
}
