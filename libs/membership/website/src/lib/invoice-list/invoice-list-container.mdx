import {Meta, Story, Canvas, CodeOrSourceMdx} from '@storybook/blocks'
import * as stories from './invoice-list-container.stories'
import LinkTo from '@storybook/addon-links/react'

<Meta of={stories} />

# Invoice List Container

> The Invoice List Container gets invoices and renders it as an <LinkTo kind="Components/InvoiceList" story="Default">InvoiceList component</LinkTo>.

## Override `InvoiceList`

To replace the `InvoiceList` component that is being rendered by a custom component, one can override it like this:

<CodeOrSourceMdx>
```typescript
import {WebsiteBuilderProvider, BuilderInvoiceListProps, InvoiceListContainer} from '@wepublish/website'

const MyInvoiceList = (props: BuilderInvoiceListProps) => <div>My Invoice List</div>

export default function () {
  return (
    <WebsiteBuilderProvider InvoiceList={MyInvoiceList}>
      <InvoiceListContainer />
    </WebsiteBuilderProvider>
  )
}
```
</CodeOrSourceMdx>

## Override `InvoiceListItem`

To replace the `InvoiceListItem` component that is being rendered by a custom component, one can override it like this:

<CodeOrSourceMdx>
```typescript
import {WebsiteBuilderProvider, BuilderInvoiceListItemProps, InvoiceListContainer} from '@wepublish/website'

const MyInvoiceListItem = (props: BuilderInvoiceListItemProps) => <div>My Invoice List Item</div>

export default function () {
  return (
    <WebsiteBuilderProvider InvoiceListItem={MyInvoiceListItem}>
      <InvoiceListContainer />
    </WebsiteBuilderProvider>
  )
}
```
</CodeOrSourceMdx>

<Canvas>
  <Story of={stories.Default} />
</Canvas>
