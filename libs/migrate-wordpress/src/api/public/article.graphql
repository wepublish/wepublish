fragment ArticleRef on Article {
    id
}

query Article($slug: Slug!) {
    article(slug: $slug) {
        id
        url
        blocks {
            __typename
            ... on RichTextBlock {
                richText
            }
        }
    }
}


