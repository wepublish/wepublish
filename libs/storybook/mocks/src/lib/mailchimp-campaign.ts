import nanoid from 'nanoid';

export const mockMailchimCampaign = ({
  id = nanoid(),
  long_archive_url = 'https=//example.com/long-archive-url',
  web_id = '',
  parent_campaign_id = '',
  type = '',
  create_time = '',
  archive_url = '',
  status = '',
  emails_sent = 0,
  send_time = '',
  content_type = '',
  needs_block_refresh = false,
  resendable = false,
  settings = mockMailchimCampaignSettings(),
  recipients = mockMailchimCampaignRecipients(),
  variate_settings = mockMailchimCampaignVariateSettings(),
  tracking = mockMailchimCampaignTracking(),
  rss_opts = mockMailchimCampaignRssOpts(),
  ab_split_opts = mockMailchimCampaignAbSplitOpts(),
  social_card = mockMailchimCampaignSocialCard(),
  report_summary = mockMailchimCampaignReportSummary(),
  delivery_status = mockMailchimCampaignDeliveryStatus(),
  _links = [],
}: any = {}) => ({
  id,
  long_archive_url,
  web_id,
  parent_campaign_id,
  type,
  create_time,
  archive_url,
  status,
  emails_sent,
  send_time,
  content_type,
  needs_block_refresh,
  resendable,
  settings,
  recipients,
  variate_settings,
  tracking,
  rss_opts,
  ab_split_opts,
  social_card,
  report_summary,
  delivery_status,
  _links,
});

export const mockMailchimCampaignSettings = ({
  subject_line = 'Lorem Ipsum #1039= This is a test subject',
  preview_text = '',
  title = '',
  from_name = '',
  reply_to = '',
  use_conversation = false,
  to_name = '',
  folder_id = '',
  authenticate = false,
  auto_footer = false,
  inline_css = false,
  auto_tweet = false,
  fb_comments = false,
  timewarp = false,
  template_id = 0,
  drag_and_drop = false,
  auto_fb_post = [],
}: any = {}) => ({
  subject_line,
  preview_text,
  title,
  from_name,
  reply_to,
  use_conversation,
  to_name,
  folder_id,
  authenticate,
  auto_footer,
  inline_css,
  auto_tweet,
  fb_comments,
  timewarp,
  template_id,
  drag_and_drop,
  auto_fb_post,
});

export const mockMailchimCampaignRecipientsSegmentOpts = ({
  saved_segment_id = 0,
  prebuilt_segment_id = '',
  match = '',
  conditions = [],
}: any = {}) => ({
  saved_segment_id,
  prebuilt_segment_id,
  match,
  conditions,
});

export const mockMailchimCampaignRecipients = ({
  list_id = '',
  list_is_active = false,
  list_name = '',
  segment_text = '',
  recipient_count = 0,
  segment_opts = mockMailchimCampaignRecipientsSegmentOpts(),
}: any = {}) => ({
  list_id,
  list_is_active,
  list_name,
  segment_text,
  recipient_count,
  segment_opts,
});

export const mockMailchimCampaignVariateSettings = ({
  winning_combination_id = '',
  winning_campaign_id = '',
  winner_criteria = '',
  wait_time = 0,
  test_size = 0,
  subject_lines = [],
  send_times = [],
  from_names = [],
  reply_to_addresses = [],
  contents = [],
  combinations = [],
}: any = {}) => ({
  winning_combination_id,
  winning_campaign_id,
  winner_criteria,
  wait_time,
  test_size,
  subject_lines,
  send_times,
  from_names,
  reply_to_addresses,
  contents,
  combinations,
});

export const mockMailchimCampaignTracking = ({
  opens = false,
  html_clicks = false,
  text_clicks = false,
  goal_tracking = false,
  ecomm360 = false,
  google_analytics = '',
  clicktale = '',
  salesforce = {
    campaign: false,
    notes: false,
  },
  capsule = {
    notes: false,
  },
}: any = {}) => ({
  opens,
  html_clicks,
  text_clicks,
  goal_tracking,
  ecomm360,
  google_analytics,
  clicktale,
  salesforce,
  capsule,
});

export const mockMailchimCampaignRssOpts = ({
  feed_url = '',
  frequency = '',
  schedule = {
    hour: 0,
    daily_send: {
      sunday: false,
      monday: false,
      tuesday: false,
      wednesday: false,
      thursday: false,
      friday: false,
      saturday: false,
    },
    weekly_send_day: '',
    monthly_send_date: 0,
  },
  last_sent = '',
  constrain_rss_img = false,
}: any = {}) => ({
  feed_url,
  frequency,
  schedule,
  last_sent,
  constrain_rss_img,
});

export const mockMailchimCampaignAbSplitOpts = ({
  split_test = '',
  pick_winner = '',
  wait_units = '',
  wait_time = 0,
  split_size = 0,
  from_name_a = '',
  from_name_b = '',
  reply_email_a = '',
  reply_email_b = '',
  subject_a = '',
  subject_b = '',
  send_time_a = '',
  send_time_b = '',
  send_time_winner = '',
}: any = {}) => ({
  split_test,
  pick_winner,
  wait_units,
  wait_time,
  split_size,
  from_name_a,
  from_name_b,
  reply_email_a,
  reply_email_b,
  subject_a,
  subject_b,
  send_time_a,
  send_time_b,
  send_time_winner,
});

export const mockMailchimCampaignSocialCard = ({
  image_url = '',
  description = '',
  title = '',
}: any = {}) => ({
  image_url,
  description,
  title,
});

export const mockMailchimCampaignReportSummary = ({
  opens = 0,
  unique_opens = 0,
  open_rate = 0,
  clicks = 0,
  subscriber_clicks = 0,
  click_rate = 0,
  ecommerce = {
    total_orders: 0,
    total_spent: 0,
    total_revenue: 0,
  },
}: any = {}) => ({
  opens,
  unique_opens,
  open_rate,
  clicks,
  subscriber_clicks,
  click_rate,
  ecommerce,
});

export const mockMailchimCampaignDeliveryStatus = ({
  enabled = false,
  can_cancel = false,
  status = '',
  emails_sent = 0,
  emails_canceled = 0,
}: any = {}) => ({
  enabled,
  can_cancel,
  status,
  emails_sent,
  emails_canceled,
});
