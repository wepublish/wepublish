export enum ContentModelSchemaTypes {
  id = 'id',
  string = 'string',
  boolean = 'boolean',
  int = 'int',
  float = 'float',
  enum = 'enum',
  dateTime = 'dateTime',
  richText = 'richText',
  reference = 'reference',
  list = 'list',
  object = 'object',
  union = 'union'
}
