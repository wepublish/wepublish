import { withLayout } from '../blocks/layout';
import { withColor } from '../blocks/color';
import { SubscribeConfig } from './subscribe.config';

export const Subscribe = withLayout(withColor(SubscribeConfig));
