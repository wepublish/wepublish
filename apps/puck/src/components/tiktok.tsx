import { withLayout } from '../blocks/layout';
import { withColor } from '../blocks/color';
import { TikTokConfig } from './tiktok.config';

export const TikTok = withLayout(withColor(TikTokConfig));
