import styled from '@emotion/styled';
import { Modal } from '@wepublish/ui';

import theme from '../theme';

export const ReflektModal = styled(Modal)`
  .MuiButton-textSecondary {
    color: ${theme.palette.text.primary};
  }
`;
