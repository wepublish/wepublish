import {createTheme} from '@mui/material'
import type {} from '@mui/x-date-pickers/themeAugmentation'

export const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(169, 119, 163)'
    }
  }
})
