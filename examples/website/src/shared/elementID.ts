export enum ElementID {
  ReactRoot = 'react-root',
  APIURL = 'api-url',
  CanonicalHost = 'canonical-host',
  RenderedPaths = 'rendered-paths',
  InitialState = 'initial-state',
  IntrospectionResult = 'introspection-result',
  MailchimpCampaign = 'mailchimp-campaign'
}
