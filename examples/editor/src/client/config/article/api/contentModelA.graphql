query ModelA($id: ID!) {
  content {
    modelA {
      read(id: $id) {
        id
        contentType
        title
        content {
          myString
          myRichText
        }
      }
    }
  }
}
